source:
  type: "postgres"
  config:
    host_port: postgres:5432
    database: datagen
    username: postgres
    password: password

sink:
  type: "datahub-rest"
  config:
    server: "http://datahub-gms:8080"